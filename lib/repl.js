class Repl {
    // TODO Can we just pass arbitrary code to the ethereum vm?
    
    /**
     * Take a string containing solidity code and return results.
     * @author Sean Batzel
     * @param {string} code - The code to be executed.
     * @returns {string} The result of running the code.
     * @public 
     */
    execute(code) {
        let result = "";
        // Pass the code through to the VM
        // Ostensibly we'll end up compiling
        // and all of that in here.
        // Get a response
        return result;
    }
}