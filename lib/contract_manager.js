// TODO: Future: accept Globs (directories of contracts, etc)
/**
 * Manages multiple contracts in one sourcecode file
 * keeps a datastructure of all contracts being debugged
 * @author Andrew Plaza <aplaza@liquidthink.net>
 * @param {Array} contracts - array of contract paths
 * @param {Logger} logger
 */
class ContractManager {
  constructor(logger, options) {
    this.contracts = 
  }

  /**
   * Returns an array of contracts
   */
  getContracts() {

  }

  /**
   * Parse a contract from its line number
   */
  parseFromLineNum() {

  }

  /**
   * add a contract to debug
   */
  add() {

  }

  /**
   * remove a contract to debug
   */
  remove() {

  }
}

module.exports = ContractManager;
