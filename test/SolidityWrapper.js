const { expect } = require('chai');
const SolidityWrapper = require('./../lib/solidity_wrapper');

describe('SolidityWrapper', function () {
    describe('#constructor(sourcePath: string, name: string)', function () {
        it('should instantiate a new SolidityWrapper without issue', function () {
        });
    });
    describe('#getSource()', function () {
        it('return the source of this contract only', function () {
        });
    });
    describe('#getLinkedSource()', function () {
        it('return the body of all contract source', function () {
        });
    });
    describe('#getImports(source: string, cwd: string, sf: string)', function () {
        it('return the array of imported contracts', function () {
        });
    });
});
